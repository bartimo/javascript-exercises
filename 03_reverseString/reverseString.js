const reverseString = function(str) {
   return Array.from(str).reverse().join("") //convert string to array, reverse it, then join with no separator.
};

// Do not edit below this line

module.exports = reverseString;
